Installation:

    git clone git@github.com:brablc/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/.vimrc ~/.vimrc

Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    # env GIT_SSL_NO_VERIFY=true
    git submodule update
	
